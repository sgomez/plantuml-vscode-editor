// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-in-docker
{
	"name": "PlantUml Dev Container",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/base:bullseye",

	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {
			"version": "latest",
			"enableNonRootDocker": "true",
			"moby": "true"
		}
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	"postStartCommand": "sh -lc 'docker ps -a --format \"{{.Names}}\" | grep -qx plantuml || docker run --rm -d --name plantuml --restart unless-stopped -p 9099:8080 plantuml/plantuml-server:jetty; docker start plantuml >/dev/null 2>&1 || true'",
	"customizations": {
		"vscode": {
			"extensions": [
				"jebbs.plantuml"
			],
			"settings": {
			"plantuml.exportFormat": "png",
			"plantuml.render": "PlantUMLServer",
			"plantuml.server": "http://localhost:9099",
    		"plantuml.diagramsRoot": "diagrams/src",
    		"plantuml.exportOutDir": "diagrams/out"
			}
		}
	}

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
